#~ proc lxir_version {} {
	#~ global LXIR
	#~ global prompt
	#~ set tmp [exec echo "version" | $LXIR]
	#~ regexp "$prompt *(\[^\n\]=)\n.*" $tmp tmp version
	#~ clone_output "[which $LXIR] version $version\n"
#~ }

proc lxir_load { arg } {
}

proc lxir_exit {} {}
proc lxir_start {} {}
